# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Go workspace file
go.work
go.work.sum

# Build artifacts
/bin/
/dist/
/cortex

# Embedded Python dependencies (generated by go generate)
# This can be ~300MB, so we don't commit it
internal/embed/server/data/

# Dependency directories
vendor/

# Go cache
.cache/

# IDE directories
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Config files (uncomment if you don't want to commit configs)
# .cortex.yaml
# config.yaml

# Cache settings (local only, not committed)
.cortex/settings.local.json

# Log files
*.log

# Temporary files
tmp/
temp/
*.bak
*.bak2

# Scratch files for experimentation or context
.scratch

# Local state for task tool
.task/

# ignore claude setup - using overwatch to manage this across projects
.claude

