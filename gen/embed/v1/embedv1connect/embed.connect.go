// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: embed/v1/embed.proto

package embedv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/mvp-joe/project-cortex/gen/embed/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// EmbedServiceName is the fully-qualified name of the EmbedService service.
	EmbedServiceName = "embed.v1.EmbedService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// EmbedServiceInitializeProcedure is the fully-qualified name of the EmbedService's Initialize RPC.
	EmbedServiceInitializeProcedure = "/embed.v1.EmbedService/Initialize"
	// EmbedServiceEmbedProcedure is the fully-qualified name of the EmbedService's Embed RPC.
	EmbedServiceEmbedProcedure = "/embed.v1.EmbedService/Embed"
	// EmbedServiceHealthProcedure is the fully-qualified name of the EmbedService's Health RPC.
	EmbedServiceHealthProcedure = "/embed.v1.EmbedService/Health"
)

// EmbedServiceClient is a client for the embed.v1.EmbedService service.
type EmbedServiceClient interface {
	// Initialize prepares the embedding server for use.
	// Downloads embedding models if needed (174MB), loads them into memory.
	// Streams progress updates during model download and loading.
	// Must be called before Embed() requests (enforced by server).
	//
	// Idempotent: Safe to call multiple times (skips download if models exist).
	//
	// Progress sequence:
	//
	//	status="checking"    → Checking if models already exist
	//	status="downloading" → Downloading models (progress 0-100%)
	//	status="loading"     → Loading embedding model into memory
	//	status="ready"       → Server ready for Embed() calls
	Initialize(context.Context, *connect.Request[v1.InitializeRequest]) (*connect.ServerStreamForClient[v1.InitializeProgress], error)
	// Embed generates embeddings for one or more texts.
	// Returns 768-dimensional float32 vectors (or truncated via config).
	//
	// Batching: Automatically handled - provide multiple texts for efficiency.
	// Mode parameter: Reserved for future (currently ignored, uses same model).
	//
	// Thread-safe: Multiple concurrent calls supported.
	// Auto-resurrection: Client retries on connection error (daemon may have idled out).
	Embed(context.Context, *connect.Request[v1.EmbedRequest]) (*connect.Response[v1.EmbedResponse], error)
	// Health returns server health and uptime statistics.
	// Used by EnsureDaemon for startup verification and idle tracking.
	Health(context.Context, *connect.Request[v1.HealthRequest]) (*connect.Response[v1.HealthResponse], error)
}

// NewEmbedServiceClient constructs a client for the embed.v1.EmbedService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewEmbedServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) EmbedServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	embedServiceMethods := v1.File_embed_v1_embed_proto.Services().ByName("EmbedService").Methods()
	return &embedServiceClient{
		initialize: connect.NewClient[v1.InitializeRequest, v1.InitializeProgress](
			httpClient,
			baseURL+EmbedServiceInitializeProcedure,
			connect.WithSchema(embedServiceMethods.ByName("Initialize")),
			connect.WithClientOptions(opts...),
		),
		embed: connect.NewClient[v1.EmbedRequest, v1.EmbedResponse](
			httpClient,
			baseURL+EmbedServiceEmbedProcedure,
			connect.WithSchema(embedServiceMethods.ByName("Embed")),
			connect.WithClientOptions(opts...),
		),
		health: connect.NewClient[v1.HealthRequest, v1.HealthResponse](
			httpClient,
			baseURL+EmbedServiceHealthProcedure,
			connect.WithSchema(embedServiceMethods.ByName("Health")),
			connect.WithClientOptions(opts...),
		),
	}
}

// embedServiceClient implements EmbedServiceClient.
type embedServiceClient struct {
	initialize *connect.Client[v1.InitializeRequest, v1.InitializeProgress]
	embed      *connect.Client[v1.EmbedRequest, v1.EmbedResponse]
	health     *connect.Client[v1.HealthRequest, v1.HealthResponse]
}

// Initialize calls embed.v1.EmbedService.Initialize.
func (c *embedServiceClient) Initialize(ctx context.Context, req *connect.Request[v1.InitializeRequest]) (*connect.ServerStreamForClient[v1.InitializeProgress], error) {
	return c.initialize.CallServerStream(ctx, req)
}

// Embed calls embed.v1.EmbedService.Embed.
func (c *embedServiceClient) Embed(ctx context.Context, req *connect.Request[v1.EmbedRequest]) (*connect.Response[v1.EmbedResponse], error) {
	return c.embed.CallUnary(ctx, req)
}

// Health calls embed.v1.EmbedService.Health.
func (c *embedServiceClient) Health(ctx context.Context, req *connect.Request[v1.HealthRequest]) (*connect.Response[v1.HealthResponse], error) {
	return c.health.CallUnary(ctx, req)
}

// EmbedServiceHandler is an implementation of the embed.v1.EmbedService service.
type EmbedServiceHandler interface {
	// Initialize prepares the embedding server for use.
	// Downloads embedding models if needed (174MB), loads them into memory.
	// Streams progress updates during model download and loading.
	// Must be called before Embed() requests (enforced by server).
	//
	// Idempotent: Safe to call multiple times (skips download if models exist).
	//
	// Progress sequence:
	//
	//	status="checking"    → Checking if models already exist
	//	status="downloading" → Downloading models (progress 0-100%)
	//	status="loading"     → Loading embedding model into memory
	//	status="ready"       → Server ready for Embed() calls
	Initialize(context.Context, *connect.Request[v1.InitializeRequest], *connect.ServerStream[v1.InitializeProgress]) error
	// Embed generates embeddings for one or more texts.
	// Returns 768-dimensional float32 vectors (or truncated via config).
	//
	// Batching: Automatically handled - provide multiple texts for efficiency.
	// Mode parameter: Reserved for future (currently ignored, uses same model).
	//
	// Thread-safe: Multiple concurrent calls supported.
	// Auto-resurrection: Client retries on connection error (daemon may have idled out).
	Embed(context.Context, *connect.Request[v1.EmbedRequest]) (*connect.Response[v1.EmbedResponse], error)
	// Health returns server health and uptime statistics.
	// Used by EnsureDaemon for startup verification and idle tracking.
	Health(context.Context, *connect.Request[v1.HealthRequest]) (*connect.Response[v1.HealthResponse], error)
}

// NewEmbedServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewEmbedServiceHandler(svc EmbedServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	embedServiceMethods := v1.File_embed_v1_embed_proto.Services().ByName("EmbedService").Methods()
	embedServiceInitializeHandler := connect.NewServerStreamHandler(
		EmbedServiceInitializeProcedure,
		svc.Initialize,
		connect.WithSchema(embedServiceMethods.ByName("Initialize")),
		connect.WithHandlerOptions(opts...),
	)
	embedServiceEmbedHandler := connect.NewUnaryHandler(
		EmbedServiceEmbedProcedure,
		svc.Embed,
		connect.WithSchema(embedServiceMethods.ByName("Embed")),
		connect.WithHandlerOptions(opts...),
	)
	embedServiceHealthHandler := connect.NewUnaryHandler(
		EmbedServiceHealthProcedure,
		svc.Health,
		connect.WithSchema(embedServiceMethods.ByName("Health")),
		connect.WithHandlerOptions(opts...),
	)
	return "/embed.v1.EmbedService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case EmbedServiceInitializeProcedure:
			embedServiceInitializeHandler.ServeHTTP(w, r)
		case EmbedServiceEmbedProcedure:
			embedServiceEmbedHandler.ServeHTTP(w, r)
		case EmbedServiceHealthProcedure:
			embedServiceHealthHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedEmbedServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedEmbedServiceHandler struct{}

func (UnimplementedEmbedServiceHandler) Initialize(context.Context, *connect.Request[v1.InitializeRequest], *connect.ServerStream[v1.InitializeProgress]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("embed.v1.EmbedService.Initialize is not implemented"))
}

func (UnimplementedEmbedServiceHandler) Embed(context.Context, *connect.Request[v1.EmbedRequest]) (*connect.Response[v1.EmbedResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("embed.v1.EmbedService.Embed is not implemented"))
}

func (UnimplementedEmbedServiceHandler) Health(context.Context, *connect.Request[v1.HealthRequest]) (*connect.Response[v1.HealthResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("embed.v1.EmbedService.Health is not implemented"))
}
